#!/bin/bash

exec chmod +x shell/app.sh;
exec chmod +x shell/appOrder.sh;
exec chmod +x shell/Transaction.sh;
exec chmod +x shell/consumer_order_created.sh;
exec chmod +x shell/consumer_order_deleted.sh;
exec chmod +x shell/consumer_transaction.sh;